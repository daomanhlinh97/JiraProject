schema{
 query:Query
 mutation:Mutation
}
type Query{
  allBooks: [Book]
  book(id: String): Book
}
type Book{
  isn:String
  title:String
  publisher:String
  author:[String]
  publishedDate:String
}
type Mutation {
  addBook(title: String!, publisher: String!, author: String!): Book
}